services:
  juice-shop:
    image: bkimminich/juice-shop:v20.0.0
    container_name: soc-lab-juice-shop
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    networks:
      - soc-lab

networks:
  soc-lab:
    driver: bridge
